generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Ticket #162 will add Article/Revision/Block models.
// This skeleton keeps Prisma wired without pretending the schema is designed.
